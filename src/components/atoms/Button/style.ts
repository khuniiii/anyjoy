import { styled } from "stitches.config";

export const ButtonStyled = styled("button", {
  backgroundColor: "#DA70D6",
  padding: "9px 16px",
  borderRadius: "6px",
  color: "#FFFFFF",
  border: "none",
  outline: "none",
  cursor: "pointer",
  fontSize: "14px",
});
