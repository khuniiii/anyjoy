import { styled } from "stitches.config";

export const TitleInput = styled("input", {
  border: "none",
  padding: "12px 15px",
  fontSize: "18px",
  width: "100%",

  "&:focus": {
    outline: "none",
  },
});
